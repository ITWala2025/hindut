[build]
  command = "npm run build"
  publish = "dist"

[context.production.environment]
  NODE_VERSION = "18"

# SPA fallback so client-side routes (react-router) work on refresh / deep links
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
